# 评测端说明

本目录不得交给候选模型。`tools/benchmark.py evaluate` 会依次运行：

1. 后端隐藏 pytest 行为矩阵；
2. 前端隐藏 Vitest 状态测试；
3. TypeScript 类型检查；
4. 少量无法稳定黑盒识别的 Vue 模板约束；
5. 受保护文件与规避物检查；
6. 过程指标和 Reviewer 人工分合成。

基线项目本来就应有大量隐藏测试失败；这不是基准损坏。`selftests/` 负责检查配置总分、打包隔离、基线分数区间和评分器行为。
